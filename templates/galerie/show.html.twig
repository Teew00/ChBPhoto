{% extends 'base.html.twig' %}

{% block title %}Galerie{% endblock %}

{% block body %}
    <h1>Galerie</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ galerie.id }}</td>
        </tr>
        <tr>
            <th>Titre</th>
            <td>{{ galerie.titre }}</td>
        </tr>
        <tr>
            <th>Photo</th>
            <td>
                <ul>
                    {% for photo in galerie.photos %}
                        <li>{{ photo.url }} — {{ photo.titre }}</li>
                    {% else %}
                        <li>Aucune photo</li>
                    {% endfor %}
                </ul>
            </td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_galerie_index') }}">back to list</a>

    <a href="{{ path('app_galerie_edit', {'id': galerie.id}) }}">edit</a>

    {{ include('galerie/_delete_form.html.twig') }}
{% endblock %}
